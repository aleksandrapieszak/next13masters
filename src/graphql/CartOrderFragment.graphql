fragment Cart on Order {
    id
    orderItems{
        id
        quantity
        total
        product{
            id
            name
            price
            images(first:1) {
                url
            }
        }
    }
}